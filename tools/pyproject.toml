[project]
name = "robot-buddy-tools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
]

[project.optional-dependencies]
sim = ["pygame>=2.5"]

[project.scripts]
robot-serial-diag = "serial_diag:main"
